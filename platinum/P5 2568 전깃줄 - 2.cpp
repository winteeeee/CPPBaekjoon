/*
#include <iostream>
#include <vector>
using namespace std;

vector<pair<int, int>> wire;
int lis[100001];
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;

    for(int i = 0; i < n; i++) {
        int a, b;
        cin >> a >> b;
        wire.push_back({a, b});
    }

    lis[0] = wire[0].second;


    return 0;
}*/
