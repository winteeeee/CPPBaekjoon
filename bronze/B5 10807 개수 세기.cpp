/*
#include <iostream>
using namespace std;

int n, v;
int arr[101];
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> n;
    for(int i = 0; i < n; i++)
        cin >> arr[i];
    cin >> v;

    int count = 0;
    for(int i = 0; i < n; i++)
        if(arr[i] == v)
            count++;

    cout << count;
    return 0;
}*/
