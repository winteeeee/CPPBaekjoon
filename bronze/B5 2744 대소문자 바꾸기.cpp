/*
#include <iostream>
#include <string>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    string temp;
    cin >> temp;

    for(int i = 0; i < temp.size(); i++) {
        if(isupper(temp[i]))
            temp[i] = tolower(temp[i]);
        else
            temp[i] = toupper(temp[i]);
    }

    cout << temp;
    return 0;
}*/
