/*
#include <iostream>
using namespace std;

long long calcFac(long long n) {
    long long result = 1;

    for (int i = 1; i <= n; i++) {
        result *= i;
    }

    return result;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    long long n;
    cin >> n;
    long long factorial = calcFac(n);
    cout << factorial / 604800;

    return 0;
}
*/
